import React from 'react'

export default function Container({children}) {
  return (
    <div className="bg-Magnolia flex justify-center items-center w-full h-full">
        {children}
    </div>

  )
}
